package DailyCode.topic278;

public class answer {
    public int firstBadVersion(int n) {
        int left =0,right = n;
        while (left<right){
            int mid =left + (right-left)/2;
//            if (isBadVersion(left)){
//                return left;
//            }
//            if (isBadVersion(right)){
//                return right;
//            }
//            if (isBadVersion(mid)){
//                return mid;
//            }

        }
        return left;
    }
}
